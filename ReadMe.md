# SentineL AI - Interactive Architecture Diagram

This is a Vite + React demo that renders an interactive architecture diagram.

## To run locally (if you want)
1. `npm install`
2. `npm run dev`

## Deployment
This project is ready to be deployed to Vercel. Use the Vercel Dashboard to import this GitHub repository. Build command: `npm run build`. Output directory: `dist`.
